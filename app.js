const coffee = require('./lib/coffee');
const user = require('./user');


const {name, price} = require('./lib/coffee');

console.log("name", name);
console.log("price", price);

console.log(coffee);
console.log(user);
